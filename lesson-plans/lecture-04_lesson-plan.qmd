---
title: "Lecture 4: Machine Learning"
subtitle: >
  {{< var course.code >}}, {{< var course.term >}}
number-sections: false
---

## Lecture overview
### Learning outcomes
- TODO

### Pre-class work
::: {.content-hidden when-profile="student" when-profile="ta"}
#### Instructors
1. TODO

#### Coordinators
1. TODO
:::

::: {.content-hidden when-profile="student"}
#### TAs
1. TODO
:::

#### Learners
1. TODO

### Lecture timeline
::: {.list-table}
- - Topic
  - Time
  - Materials

- - Admin
  - 5 min

- - [Differences from previous lectures](#sec-prev-lectures)
  - 5 min

- - [Credit card fraud detection](#sec-fraud-detection)
    - Features and labels
    - Linear models
  - 20 min

- - [Student work-life balance](#sec-work-life)
    - Decision trees
  - 10 min

- - [Activity: Optimization](#sec-optimization)
  - 20 min
  - Tool TODO

- - [Wrap-up](#sec-wrap-up)
  - 10 min

- - Admin
  - 5 min
:::

### Post-class work
::: {.content-hidden when-profile="student" when-profile="ta"}
#### Instructors
1. TODO

#### Coordinators
1. TODO
:::

::: {.content-hidden when-profile="student"}
#### TAs
1. TODO
:::

#### Learners
1. TODO

## Differences from previous lectures{#sec-prev-lectures}
:::{.callout-tip}
### Activity: Discussion
- What kinds of problems would be hard to solve with explicit programming or search?
:::
- We do not always want to use pre-defined, set rules like we did in explicit rule-based programming.
- In machine learning, we build a model that will learn the rules from the data.

## Credit card fraud detection{#sec-fraud-detection}
- Have you ever had your credit card declined on vacation? You were probably interacting with a credit card fraud detection model!
- Being able to identify fraudulent transactions is useful: it protects businesses and credit card holders from financial loss after fraud attempts happen.
- Billions of transactions are processed every day, so being able to identify these transactions programmatically is even better.

:::{.callout-tip}
### Activity: Discussion
What information could be used to identify whether a single transaction is fraudulent or not? [TODO: any way to make it clearer that they need to think IID?]
:::

### Features and labels
- Data used to identify whether a transaction is fraudulent could include:

::: {.list-table}
- - User ID
  - Amount ($)
  - Distance from Home (km)
  - Account Age (days)
  - Fraud?

- - 1
  - 120
  - 25
  - 1190
  - Legit
- - 2
  - 100
  - 90
  - 606
  - Fraud
- - 3
  - 105
  - 55
  - 582
  - Fraud
- - 4
  - 145
  - 0
  - 524
  - Legit
:::

- Each row in the table above is an **example**. Each example has **features** and **labels**. 
  - Features are things we already know about an example, like User ID, Amount, Distance from Home, and Account Age.
  - Labels are things we would like to predict, like Fraud.
  - We represent the features with X and represent the labels with Y.
- We **train** a **model** on lots of examples with labels we already know so that the model can learn to **predict** labels for new examples.

:::{.callout-tip}
### Activity: Discussion
How could you use each feature in the table to predict fraud? Are all of the features useful?
:::
- If you have lots of features, manually coming up with a rule for every possible example the model might see is not reasonable! With machine learning, the model learns the rules for you.
- Open questions: What kind of rules does the model learn? How do the rules relate to the features and labels?

### Linear models
- We want to know whether a transaction is fraudulent or not based only on features. 
- An easy way to do this is to use a **linear model**: we draw a 2D line (or 3D plane) between the two types of transactions, say that everything on one side is fraudulent, and say that everything on the other side is legitimate. [TODO: phrase this better, they might be intimidated by 2D vs 3D/planes]

:::{.callout-tip}
### Activity: Discussion
Let's consider two different options: a line that only uses Amount and Distance from Home, and a plane that also uses Account Age. Which option do you prefer? Why?
:::

```{python}
#| echo: false
%run lesson-plan-code/lecture-04_boundaries.py
import pandas as pd

# colours chosen from https://davidmathlogic.com/colorblind/
# datasets fabricated using Claude Fable 5
card_data = pd.read_csv('lesson-plan-data/card_transactions.csv')
color_map_card = {'Legit': '#5D3A9B', 'Fraud': '#E66100'}
boundary_plot_2d_to_3d(card_data, 'amount', 'distance_from_home', 'account_age', 'label', title='Fraud or Legit?', color_map=color_map_card, 
                        line=((0, 100), (300, 0)), plane=(1/60, 1/25, -1/350, -1.8))
```

- The more information we have about the transactions, the easier it is to draw a line that separates them. 

:::{.callout-tip}
### Activity: Discussion
Let's consider two more planes. Which one do you prefer? Why?
:::

```{python}
#| echo: false

boundary_plot_compare_3d(card_data, 'amount', 'distance_from_home', 'account_age', 'label', title='Fraud or Legit?', color_map=color_map_card, 
                        separator_a=(1/60, 1/25, -1/1050, -3.8), separator_b=(1/60, 1/25, -1/350, -1.8))
```

- Some planes represent the relationship between features and labels better than others. We want to find the best one possible so that we can make good predictions.
- Lines and planes are represented by **parameters**. During the training process, the linear model tries to learn the parameters that give us the best one.

## Student work-life balance{#sec-work-life}
- Let's consider another scenario: we have a list of students, and we want know whether they have a good or bad work-life balance.

:::{.callout-tip}
### Activity: Discussion
What might be some features we could use to make this prediction? What would the label be?
:::

- The features we'll use are hours spent doing coursework and hours spent doing social activities.
- The label will be the group the student should belong to: balanced or imbalanced.
- Let's try to draw some lines to separate the two groups based on these features.

```{python}
#| echo: false
balance_data = pd.read_csv('lesson-plan-data/balance_data.csv')
color_map_balance = {'balanced': '#0C7BDC', 'imbalanced': '#FFC20A'}
boundary_plot_compare_2d(balance_data, 'coursework_hours', 'social_hours', 'group', separator_a={"coef": (1, 0), "intercept": -15, "name": "line 1"},
    separator_b={"coef": (0, 1), "intercept": -15, "name": "line 2"}, title='Student Work-Life Balance', color_map=color_map_balance)
```

:::{.callout-tip}
### Activity: Discussion
Neither line we drew does a great job of separating the two groups. What kind of shape might do a better job?
:::

### Decision trees
- Instead of using a linear model, we could use a **decision tree** to separate the two groups.
- A decision tree is like a 20 questions game: we ask a series of yes/no questions about the features to determine which group a student belongs to.
- [TODO: image of decision tree/20 questions game? Instructor plays 20 questions with students?]

:::{.callout-tip}
### Activity: Discussion
What kinds of questions are more useful to ask at the start of a 20 questions game? What about later in the game?
:::

- Different questions will give us different decision trees, and some will do a better job of separating labels than others. 
- Instead of finding the line or plane that best separates the labels, a decision tree finds the questions that best separate the labels at each step of the game. These questions are the parameters of the decision tree.

```{python}
#| echo: false
from sklearn.tree import DecisionTreeClassifier
model = DecisionTreeClassifier(max_depth=4).fit(balance_data[['coursework_hours', 'social_hours']], balance_data['group'])
boundary_plot_compare_2d(balance_data, 'coursework_hours', 'social_hours', 'group', separator_a={"coef": (1, 0), "intercept": -15, "name": "linear model"},
    separator_b={"model": model, "name": "decision tree"}, title='Student Work-Life Balance', color_map=color_map_balance)
```

## Activity: Optimization{#sec-optimization}
- TODO activity - needs a tool
  - They pick a line, then new data is added and they see that another line might be better 
  - Should show them that they can't necessarily rely on the training data/limits of training data - generalizability
  - [TODO: any slides needed about train/test data?]

## Wrap-up{#sec-wrap-up}

:::{.callout-tip}
### Activity: Discussion
- What real-life applications of machine learning do you know? What features and labels might those models use?
- Beyond lines/planes and decision trees, what other boundary shapes might be useful to have?
- What makes one model better than another model?
- [TODO: another question about generalizability?]
:::

